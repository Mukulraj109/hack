VITE_API_BASE_URL=http://localhost:5000
# Shared Auth0 SPA with FirstStep — add http://localhost:5173 and http://localhost:5174 to Allowed Callback URLs
VITE_AUTH0_DOMAIN=dev-app0lbs2pjcoxxuv.us.auth0.com
VITE_AUTH0_CLIENT_ID=x2ginS1tSrJizlvIdyQLzeEfWfGFFbd9
VITE_AUTH0_AUDIENCE=https://dev-app0lbs2pjcoxxuv.us.auth0.com/api/v2/
VITE_AUTH0_CALLBACK_URL=http://localhost:5173
# FirstStep frontend — used for cross-app "Claim Your Spot" login redirect (Option A flow)
# Local dev: FirstStep runs on port 5174; production: https://firststepjob.com
VITE_FIRSTSTEP_CALLBACK_URL=http://localhost:5174/
VITE_FIRSTSTEP_DASHBOARD_URL=http://localhost:5174/dashboard
VITE_FIRSTSTEP_ORIGIN=http://localhost:5174
# Hackathon registration form URL is configured on the backend (ZOHO_REGISTRATION_FORM_URL).
# Social share verification form is configured on the backend (ZOHO_SOCIAL_SHARE_FORM_URL).
# Submission tab — ZIP files are emailed here (not uploaded in-app)
# VITE_HACKATHON_SUBMISSION_ZIP_EMAIL=Hackathon@firststepjob.com
# Hiring partner form URL is configured on the backend (ZOHO_HIRING_PARTNER_FORM_URL).
# Information session form URL is configured on the backend (ZOHO_INFO_SESSION_FORM_URL).
# Sprint portal — per-track brief/starter asset URLs (optional; shown after sprint starts)
# VITE_TRACK_ASSET_PREDICTIVE_CAREER_INTELLIGENCE=
# VITE_TRACK_ASSET_JOB_MARKET_VISUALIZED=
# VITE_TRACK_ASSET_SMART_APPLICATION_TRACKER=
# VITE_TRACK_ASSET_DEPLOY_AT_SCALE=
# VITE_TRACK_ASSET_SECURE_BY_DEFAULT=
